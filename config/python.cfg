include=from collections import deque\nfrom sys import stdin\n\n
main_func_begin=def main():\n    inputs = deque()\n    for line in stdin.readline():\n        for token in line.split(' '):\n            token = token.strip()\n            if len(token) != 0:\n                inputs.appendleft(token)\n\n
main_func_end=\nmain()\n
default_type=None
struct_begin={{shift}}class {{type}}():\n{{shift}}    pass\n\n
struct_end=
array_declaration=
var_declaration=
indentation=    
array_init={{shift}}{{name}} = [{{type}}] * {{size}}\n
loop_begin={{shift}}for {{iterator}} in range({{size}}):\n
loop_end=
fetch_var={{shift}}{{name}} = int(inputs.pop())\n
fetch_array={{shift}}    {{name}}[{{iterator}}] = int(inputs.pop())\n
array_element={{name}}[{{iterator}}].
struct_element=.
